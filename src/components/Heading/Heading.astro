---
// import "./Heading.scss";
import type { HeadingProps } from "./Heading.ts";

interface Props extends HeadingProps {
  class?: string;
  tag?: string;
}

const { tag: Tag, ...attributes } = Astro.props;

---

<Tag class={`Heading ${attributes.class ?? ""}`}>
  <slot />
</Tag>

<script src="./Heading.client.ts"></script>